services:
   _defaults:
      autoconfigure: true
      autowire: true
      public: false
      bind:
          $pageFinder: '@contao.routing.page_finder'
          $requestStack: '@request_stack'
          $contaoGeneralLogger: '@monolog.logger.contao.general'

   Cgoit\RobotsTxtBundle\:
      resource: ../src/*
